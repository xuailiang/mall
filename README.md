# 🛍️ 京东风格商城前端

一个基于 Vue3 + Vite 的现代化移动端电商应用脚手架，内置 NutUI 组件库和京东风格设计规范。

## ✨ 主要特性

- 🎯 **Vue 3 Composition API** - 使用最新的 Vue 3 组合式 API 编写，代码更清晰易维护
- ⚡ **Vite 构建** - 极速开发和构建体验，热更新速度超快
- 🎨 **NutUI 组件库** - 集成京东开源的 NutUI 移动组件库，开箱即用
- 🎭 **京东设计规范** - 内置设计 Token，保持视觉一致性
- 📱 **移动端优化** - 完全为移动端设计，响应式布局
- 💼 **业务组件** - 内置电商常用组件（价格标签、促销徽章、商品卡片等）

## 🚀 快速开始

### 1️⃣ 环境要求

- Node.js >= 14.x
- npm >= 6.x 或 yarn

### 2️⃣ 安装依赖

```bash
npm install
```

或者使用 yarn：

```bash
yarn install
```

### 3️⃣ 启动开发服务器

```bash
npm run dev
```

开发服务器将在 `http://localhost:5173` 启动��支持热模块替换 (HMR)

### 4️⃣ 构建生产版本

```bash
npm run build
```

优化后的生产版本将生成在 `dist` 目录中

### 5️⃣ 预览生产构建

```bash
npm run preview
```

## 📁 项目结构

```
src/
├── components/          # 业务组件库
│   ├── PriceTag.vue        # 价格标签组件
│   ├── PromoBadge.vue      # 促销徽章组件
│   └── ProductCard.vue     # 商品卡片组件
│
├── pages/              # 页面组件
│   ├── Home.vue            # 首页
│   ├── Product.vue         # 商品详情页
│   ├── Cart.vue            # 购物车页面
│   └── Profile.vue         # 用户中心页面
│
├── styles/             # 样式和设计规范
│   ├── theme.css           # 颜色、字体等主题变量
│   ├── base.css            # 全局基础样式
│   └── sections.css        # 页面分区样式
│
├── App.vue             # 根组件
└── main.js             # 应用入口

index.html             # HTML 入口文件
vite.config.js        # Vite 配置文件
package.json          # 项目依赖和脚本配置
```

## 🎨 设计规范

项目包含完整的京东风格设计规范，所有颜色、字体、间距等都已预定义：

### 主要样式文件

| 文件 | 说明 |
|-----|------|
| `src/styles/theme.css` | 主题变量（颜色、尺寸、字体等） |
| `src/styles/base.css` | 全局基础样式重置 |
| `src/styles/sections.css` | 页面分区组件样式 |

### 快速应用主题

在你的组件中直接使用 CSS 变量：

```vue
<style scoped>
.button {
  background-color: var(--primary-color);
  color: var(--text-primary);
  padding: var(--spacing-md);
  border-radius: var(--radius-md);
}
</style>
```

## 📄 页面模块说明

### 📱 首页 (Home.vue)
- 商品列表展示
- 分类导航
- 轮播图
- 推荐商品

### 🛒 商品详情页 (Product.vue)
- 商品图片展示
- 规格选择
- 价格显示
- 加入购物车

### 🛍️ 购物车 (Cart.vue)
- 购物车商品列表
- 商品数量编辑
- 价格计算
- 结算功能

### 👤 用户中心 (Profile.vue)
- 用户信息展示
- 订单历史
- 地址管理
- 个人设置

## 🧩 业务组件

### 价格标签 (PriceTag.vue)

显示商品价格，支持原价和现价对比：

```vue
<PriceTag :price="99.99" :originalPrice="129.99" />
```

### 促销徽章 (PromoBadge.vue)

显示促销标签，如"限时折扣"、"新品上市"：

```vue
<PromoBadge type="discount" text="5折" />
```

### 商品卡片 (ProductCard.vue)

完整的商品卡片组件，包含图片、标题、价格、评价等：

```vue
<ProductCard 
  :product="{ 
    id: 1, 
    title: '商品名称', 
    price: 99.99,
    image: 'url'
  }" 
  @click="handleProductClick"
/>
```

## 🛠️ 常用命令

```bash
# 开发模式下运行
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 格式化代码（如果配置了）
npm run format

# 代码检查（如果配置了）
npm run lint
```

## 📚 技术栈

| 技术 | 版本 | 说明 |
|-----|------|------|
| Vue | 3.x | 渐进式 JavaScript 框架 |
| Vite | 最新 | 下一代前端构建工具 |
| NutUI | 最新 | 京东开源移动组件库 |
| JavaScript | ES6+ | 现代 JavaScript 语法 |
| CSS3 | 最新 | 现代 CSS3 特性 |

## 🔗 相关资源

- [Vue 3 官方文档](https://vuejs.org/)
- [Vite 官方文档](https://vitejs.dev/)
- [NutUI 文档](https://nutui.jd.com/)
- [京东设计规范](https://jdux.jd.com/)

## 📝 开发建议

### 文件命名规范

- 组件文件：使用 PascalCase，如 `ProductCard.vue`
- 页面文件：使用 PascalCase，如 `Home.vue`
- 样式文件：使用 kebab-case，如 `product-card.css`
- JavaScript 文件：使用 camelCase，如 `utils.js`

### 组件开发指南

1. 在 `src/components` 目录创建新组件
2. 使用 Composition API 编写逻辑
3. 在 `src/styles` 中引入相应样式
4. 在页面中引入并使用组件

### 状态管理

- 简单状态：使用 Vue 3 响应式系统（`ref`、`reactive`）
- 复杂状态：推荐使用 Pinia 或 Vuex

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request 来帮助改进这个项目！

## 📄 许可证

本项目采用 MIT 许可证

---

**最后更新**: 2026年2月11日

如有问题或建议，欢迎通过 Issue 与我们联系！